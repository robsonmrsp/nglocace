import { Page, Config} from '';
export class PageGeneroComponent<> {

    page: pa
    config: {
        page: 1,
        pageSize: 10,
        totalPages: null,
        orderBy: 'id',
        direction: 'desc',
    }
}
